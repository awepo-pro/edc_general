你的任务是将给定的文本转换为语义图，形式为三元组列表。每个三元组的格式必须为 [实体1，关系，实体2]。在你的回答中，请严格只包含三元组，不要包含任何解释或道歉。所有输出必须为简体中文。
以下是一些示例:
{few_shot_examples}

现在请从以下文本中提取三元组。以下是一些你在提取过程中可以参考的潜在关系及其描述:
{relations_hint}
请注意，此列表可能并不完整，你可以使用其他关系，文本中也不一定包含此列表中的所有关系。
文本: {input_text}
候选实体: {entities_hint}
